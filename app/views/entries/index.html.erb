<h4>My Entries</h4>

<div class="row">
  <div class="card">
    <div class="card-body">
    <% if @entries.any? %>
      <%= render(@entries) %>
    <% else %>
      <p>You have no entries yet. <%= link_to 'Create one now!', new_entry_path %></p>
    <% end %>
    </div>
  </div>
</div>